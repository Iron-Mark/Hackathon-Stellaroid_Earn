# Archived docs-new Workspace

`docs/archive/docs-new/` is a staging area, not the canonical product source of truth.

Use the root `README.md`, `ROADMAP.md`, `MAINTENANCE.md`, `docs/README.md`, and the current `frontend/` and `contract/` code for verified project state.

## Keep

- `bootcamp-ops/participant-checklist.md` - useful historical bootcamp operations checklist.
- `bootcamp-ops/program-flow.pdf` - keep only if it is the final participant artifact.
- `specs/stellaroid-earn-spec.md` - useful origin/spec artifact, but older than the current implementation.
- `specs/dapp-idea-prompt-template.md` - useful prompt/template artifact.

## Scratch / Research

Scratch research with raw citation tokens, modeled planning inputs, market claims, and future-scope NFT ideas was pruned from this archive during the docs cleanup. Keep this archive focused on bootcamp operations and origin prompt/spec artifacts.

## Do Not Commit

- `_credentials/`
- local recovery notes
- testnet secret-key bundles
- generated screenshots or browser artifacts

Local credentials that used to sit under `_credentials/` were moved to `.local/credentials/docs-new/`, which is intentionally ignored by `.gitignore`.
